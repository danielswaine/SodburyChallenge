<tr>
  <td><%= user.name %></td>
  <td><%= user.email %></td>
  <td><%= time_ago_in_words(user.created_at).humanize %> ago</td>
  <td>
    <% if current_user?(user) %>
      <%= link_to "Manage", edit_user_path(user) %>
    <% else %>
      <%= link_to "Remove", user, method: :delete,
                                  data: { confirm: "Remove #{user.name}? This cannot be undone." } %>
    <% end %>
  </td>
</tr>
